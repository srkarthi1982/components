---
import ComponentsLayout from "../layouts/ComponentsLayout.astro";
import ComponentDocDetail from "../components/docs/ComponentDocDetail.astro";
import { componentDocs } from "../data/componentDocs";

const doc = componentDocs.find((entry) => entry.slug === "av-code");
if (!doc) {
  throw new Error("Component data missing: av-code");
}

const pageTitle = `${doc.name} — Ansiversa Components`;
---

<ComponentsLayout title={pageTitle}>
  <ComponentDocDetail doc={doc} />
</ComponentsLayout>
